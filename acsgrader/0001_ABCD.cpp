#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio();
    cin.tie(NULL);
    cout.tie(NULL);

    unsigned long long a, b, c, d;
    cin >> a >> b >> c >> d;
    cout << a + b + c + d << endl;

    return 0;
}
